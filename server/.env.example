# Neon Auth base URL (same as frontend; used for JWKS and issuer validation)
NEON_AUTH_BASE_URL=
# WebSocket server port (default 8080)
WS_PORT=
# Postgres connection string (Neon). Leave empty to disable game history. Do not commit the real URL.
DATABASE_URL=
