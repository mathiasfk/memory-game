# Neon Auth base URL (from Neon Console → Auth → Configuration)
VITE_NEON_AUTH_URL=
# WebSocket server URL (e.g. ws://localhost:8080/ws for local dev)
VITE_WS_URL=
# API base URL for history (optional; defaults to same host as VITE_WS_URL)
# VITE_API_URL=
